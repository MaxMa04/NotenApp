<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ViewModels="clr-namespace:NotenApp.ViewModels"
             x:Class="NotenApp.Pages.DetailSeite"
             x:Name="detailseite">
    <ContentPage.BindingContext>
        <ViewModels:DetailViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <Grid BackgroundColor="#303030">

            <!--Griddefinition-->

            <Grid.RowDefinitions>
                <RowDefinition Height="200" />
                <RowDefinition Height="80" />
                <!--Beginn Notenverwaltung-->
                <RowDefinition Height="50"/> 
                <RowDefinition Height="45" />
                <RowDefinition Height="50"/>
                <RowDefinition x:Name="rdcv" Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
             <!--Aktueller Schnitt-->
            <StackLayout Grid.Row="0" Grid.ColumnSpan="3">
                <Frame HeightRequest="120" WidthRequest="120" CornerRadius="80" HorizontalOptions="Center" VerticalOptions="Center" Margin="0,20,0,0" Padding="0" BackgroundColor="#7BA4A8" >
                    <Label Text="{Binding FachDurchschnitt ,TargetNullValue='-'}" TextColor="White" HorizontalOptions="Center" VerticalOptions="Center" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" FontSize="50" />
                </Frame>
                <Label Text="{Binding FachEinzubringendeHalbjahre}" TextColor="White" HorizontalOptions="Center" VerticalOptions="Center" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" FontSize="30" />
                <Label Text="{Binding FachName}" TextColor="White" HorizontalOptions="Center" VerticalOptions="Center" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" FontSize="30" />
            </StackLayout>
            <!--Facheinstellungen-->
            <Button Grid.Row="1" Grid.Column="0" Text="Runden"/>
            <Button Grid.Row="1" Grid.Column="1" Text="Ziel"/>
            <Button Grid.Row="1" Grid.Column="2" Text="Rausstreichen"/>
            
            <!--Klausur Noten Übersicht-->
            <Frame BackgroundColor="Red" Grid.Row="2" Grid.ColumnSpan="3" Margin="10,0,10,-6">
                <StackLayout Orientation="Horizontal">
                    <Label Margin="0,-10" Text="Klausur-Noten" FontSize="Medium" VerticalOptions="Center"/>
                    <ImageButton VerticalOptions="Center" Margin="0,-10" Source="Plus1.png" Clicked="AddKlausurNote"></ImageButton>
                </StackLayout>
            </Frame>
            <CollectionView SelectionMode="Single" SelectionChanged="DeleteNote" Background="LightGreen" Margin="10,0,10,-15" BackgroundColor="White" Grid.Row="3" Grid.ColumnSpan="3" ItemsSource="{Binding KlausurNoten}">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout
                        Orientation="Vertical"
                        Span="6"
                        VerticalItemSpacing="5"
                        HorizontalItemSpacing="3"/>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Margin="5" CornerRadius="20" HeightRequest="9">
                            <Label TextColor="Black" Margin="-10" VerticalOptions="Center" HorizontalOptions="Center" Text="{Binding Note}" FontSize="Medium"></Label>
                        </Frame>

                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            
            <!--LK Noten Übersicht-->
            <Frame BackgroundColor="Red" Grid.Row="4" Grid.ColumnSpan="3" Margin="10,10,10,-16">
                <StackLayout Orientation="Horizontal">
                    <Label Text="LK-Noten" Margin="0,-10" FontSize="Medium" VerticalOptions="Center"/>
                    <ImageButton VerticalOptions="Center" Margin="0,-10" Source="Plus1.png" Clicked="AddLKNote"></ImageButton>
                </StackLayout>
            </Frame>
            <CollectionView x:Name="cv" Margin="10" SelectionMode="Single" SelectionChanged="DeleteNote" BackgroundColor="Aqua" Grid.Row="5" Grid.ColumnSpan="3" ItemsSource="{Binding LKNoten}">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout
                        Orientation="Vertical"
                        Span="6"
                        VerticalItemSpacing="5"
                        HorizontalItemSpacing="3"/>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Margin="5" CornerRadius="20" HeightRequest="9">
                            <Label TextColor="Black" Margin="-10" VerticalOptions="Center" HorizontalOptions="Center" Text="{Binding Note}" FontSize="Medium"></Label>
                        </Frame>
                        
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </Grid>
    </ContentPage.Content>
</ContentPage>