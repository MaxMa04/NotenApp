<?xml version="1.0" encoding="utf-8" ?>
<xctViews:Popup
    x:Class="NotenApp.Pages.EntscheidungsPopup"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:xctViews="clr-namespace:Xamarin.CommunityToolkit.UI.Views;assembly=Xamarin.CommunityToolkit"
    HorizontalOptions="CenterAndExpand"
    Size="370,80"
    VerticalOptions="EndAndExpand">
    <Grid Padding="10,15" BackgroundColor="{DynamicResource Primary}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Button
            Grid.Column="0"
            BackgroundColor="{DynamicResource Secondary}"
            Clicked="LK_Button_Clicked"
            CornerRadius="20"
            FontSize="20"
            Text="Lk"
            TextColor="{DynamicResource Fith}" />
        <Button
            Grid.Column="1"
            BackgroundColor="{DynamicResource Secondary}"
            Clicked="Klausur_Button_Clicked"
            CornerRadius="20"
            FontSize="20"
            Text="Klausur"
            TextColor="{DynamicResource Fith}" />
    </Grid>
</xctViews:Popup>




